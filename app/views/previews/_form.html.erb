<%= form_with(model: ([@product, preview]), local: true) do |form| %>
  <% if preview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preview.errors.count, "error") %> prohibited this preview from being saved:</h2>

      <ul>
      <% preview.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <div id="star-rating"></div>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :preview_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>



<script>
    $('#star-rating').raty({
      path: '/assets/',
      scoreName: 'preview[rating]'
    });
  </script>